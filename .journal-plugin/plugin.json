{
    "name": "code-commandments",
    "version": "1.39.0",
    "title": "Code Commandments",
    "description": "The static code checker judges the codebase, its hooks run inside the journal, and every rule and language is a switch in its settings",
    "journal": "2.119.0",
    "events": {
        "sin-found": {
            "title": "Sin found",
            "tone": "warn",
            "card": {
                "label": "Sin found",
                "icon": "warn"
            }
        },
        "sin-resolved": {
            "title": "Sin repented",
            "tone": "good",
            "card": {
                "label": "Sin repented",
                "icon": "check"
            }
        }
    },
    "requires": {
        "php": {
            "check": "php -v",
            "hint": "code-commandments runs on PHP 8.4 or newer"
        },
        "composer": {
            "check": "composer --version",
            "hint": "composer installs the checker inside the plugin's own folder"
        }
    },
    "env": {
        "CLAUDE_PROJECT_DIR": "{project}"
    },
    "setup": [
        {
            "name": "Install the checker in the plugin's folder",
            "run": "composer install --no-interaction --no-progress --quiet"
        },
        {
            "name": "Write the chosen switches into config.php",
            "run": "bin/commandments journal-config"
        },
        {
            "name": "Check for sins, on demand",
            "run": "journal check create \"Check for sins\" --set command=\".journal/plugins/code-commandments/bin/commandments judge\" --set every=0 --set failure=\"Code Commandments found {summary}\""
        }
    ],
    "refuse": "bin/commandments journal-hook",
    "refuse_socket": "hooks",
    "reads": true,
    "refuse_seconds": 20,
    "on": {
        "hook.PostToolUse": "bin/commandments journal-hook",
        "hook.Stop": "bin/commandments journal-hook",
        "hook.SessionStart": "bin/commandments journal-hook",
        "hook.SubagentStart": "bin/commandments journal-hook",
        "plugin.updated": "bin/commandments journal-config"
    },
    "settings": {
        "folders_judged": {
            "title": "Folders to check",
            "help": "One folder per line, from the project root. Empty, config.php keeps the folders it names.",
            "type": "list",
            "default": "",
            "group": "Folders"
        },
        "folders_skipped": {
            "title": "Folders to leave out",
            "help": "One folder per line, never read or reported, such as generated code. This list is what config.php leaves out.",
            "type": "list",
            "default": "",
            "group": "Folders"
        },
        "language_php": {
            "title": "PHP",
            "help": "Enables PHP sin detection",
            "type": "flag",
            "default": "true",
            "group": "Languages"
        },
        "language_vue": {
            "title": "Vue",
            "help": "Enables Vue sin detection",
            "type": "flag",
            "default": "true",
            "group": "Languages"
        },
        "language_ts": {
            "title": "TypeScript",
            "help": "Enables TypeScript sin detection",
            "type": "flag",
            "default": "true",
            "group": "Languages"
        },
        "language_py": {
            "title": "Python",
            "help": "Enables Python sin detection",
            "type": "flag",
            "default": "true",
            "group": "Languages"
        },
        "language_cs": {
            "title": "C#",
            "help": "Enables C# sin detection",
            "type": "flag",
            "default": "true",
            "group": "Languages"
        },
        "sin_blank-string-default": {
            "title": "BlankStringDefault",
            "help": "A declaration typed as a TOTAL `string` defaulting to the blank, whose own scope then asks that name `=== ''` / `empty(...)` — the question is the proof that the blank is absence wearing a total type.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_blank-string-on-the-wire": {
            "title": "BlankStringOnTheWire",
            "help": "A public field typed as a TOTAL `string` whose reader on the FAR SIDE asks it `=== ''`.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_cancelled-coalesce": {
            "title": "CancelledCoalesce",
            "help": "A manufactured fake compared against itself — `($x ?? '') !== ''`.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_conditional-array-spread": {
            "title": "ConditionalArraySpread",
            "help": "Flags a conditional array-element spread — `...($x ? ['k' => $x] : [])` inside an array literal, or `array_merge($base, $cond ? [...] : [])` — the ternary-into-empty-array noise.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_de-nulled-finder": {
            "title": "DeNulledFinder",
            "help": "Detects a nullable finder whose result is de-nulled at every call site (≥2 sites); absence should be decided at the source, not re-checked everywhere.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_erased-null-object": {
            "title": "ErasedNullObject",
            "help": "A `new X` whose `__toString` renders `''`, written where the declared type is `string` — a default or a return.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_nullable-callback": {
            "title": "NullableCallback",
            "help": "A nullable callback (`?callable $cb = null`) that the body null-normalises before calling — `if ($cb !== null) { $cb(…); }`, `($cb ?? fn () => …)(…)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_option-as-nullable": {
            "title": "OptionAsNullable",
            "help": "Detects `?Option`, `Option | null`, and `unwrapOr(null)` collapsing absence; exempt in argument position, flagged only in return/assignment.",
            "type": "flag",
            "default": "true",
            "group": "backend/absence",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_flag-argument": {
            "title": "FlagArgument",
            "help": "A method whose whole body is `if ($flag) {…} else {…}` on one of its own `bool` parameters — two methods sharing a name, and a call site that reads `render($order, true)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/behaviour-per-method",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_member-after-method": {
            "title": "MemberAfterMethod",
            "help": "Detects state declared below behaviour: a trait use, constant, property, property hook or enum case that sits after a method in the same class body.",
            "type": "flag",
            "default": "true",
            "group": "backend/class-layout",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_member-out-of-order": {
            "title": "MemberOutOfOrder",
            "help": "Detects a head of class assembled in an ad-hoc order — a constant under a property, a public field under a private one, a derived hook above the fields it reads.",
            "type": "flag",
            "default": "true",
            "group": "backend/class-layout",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_concurrent-subclass": {
            "title": "ConcurrentSubclass",
            "help": "A class that `extends` the `jessegall/concurrent` package's `Concurrent` proxy — inheriting the thread-safe shared-state wrapper instead of composing it.",
            "type": "flag",
            "default": "true",
            "group": "backend/concurrent-state",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_namespace-cycle": {
            "title": "NamespaceCycle",
            "help": "Two namespaces that reference each other — `App\\Billing` reaching into `App\\Orders` while `App\\Orders` reaches back — so the pair is one unit wearing two names.",
            "type": "flag",
            "default": "true",
            "group": "backend/dependency-direction",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_namespace-dependency": {
            "title": "NamespaceDependency",
            "help": "A reference OUT of a declared layer into a layer that layer may not use — the arrow pointing back up the stack, or sideways.",
            "type": "flag",
            "default": "true",
            "group": "backend/dependency-direction",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_archaeology-comment": {
            "title": "ArchaeologyComment",
            "help": "Detects comments describing history instead of present code.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_bloated-docblock": {
            "title": "BloatedDocblock",
            "help": "A class whose docblock runs to multiple paragraphs.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_ceremony-docblock": {
            "title": "CeremonyDocblock",
            "help": "A docblock that only restates the typed signature — `@param Type $x` with no description on an already-typed parameter, plus maybe a bare `@return Type`.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_dangling-doc-reference": {
            "title": "DanglingDocReference",
            "help": "A docblock cross-reference (`…` / `{@link …}`) that points at a FIRST-PARTY class the codebase does not declare — a name that was renamed or removed, its documentation left dangling.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_inline-docblock": {
            "title": "InlineDocblock",
            "help": "Detects a docblock crammed onto its delimiters — the whole thing on one line, or a block that opens or closes beside its text.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_negative-space-comment": {
            "title": "NegativeSpaceComment",
            "help": "Detects comments that defend code against strawmen — a negation paired with a word like \"random\", \"magic\", or \"coincidence\", where the code should simply state what it IS.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_restated-comment": {
            "title": "RestatedComment",
            "help": "Detects an inline comment that only narrates the statement it sits on: reduce the comment to its content words and the annotated statement's head to the words it spells (codeWords()), and flag when EVERY comment word is already one of them — a comment carrying a why brings a word the code lacks, so it survives.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_stacked-docblock": {
            "title": "StackedDocblock",
            "help": "Detects a declaration wearing several docblocks.",
            "type": "flag",
            "default": "true",
            "group": "backend/documentation",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_const-class-enum": {
            "title": "ConstClassEnum",
            "help": "A class that is nothing but scalar constants — a closed set of values hand- rolled as `const STATUS_PENDING = 'pending'` instead of a native backed enum.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_enum-case-or-chain": {
            "title": "EnumCaseOrChain",
            "help": "`$x === Status::Pending || $x === Status::Paid` — a hand-rolled membership test against two-or-more cases of the same backed enum.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_enum-value-match": {
            "title": "EnumValueMatch",
            "help": "Detects `match`/`switch` over a backed enum's `->value` at a call site.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_in-array-mirrors-enum": {
            "title": "InArrayMirrorsEnum",
            "help": "`in_array($x, ['a', 'b', …])` whose literals ARE an existing backed enum's case values — testing membership of a set the type already seals.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_match-default-returns-null": {
            "title": "MatchDefaultReturnsNull",
            "help": "A `match` whose `default` arm returns `null`/`false`/`[]` instead of throwing.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_string-match-mirrors-enum": {
            "title": "StringMatchMirrorsEnum",
            "help": "Detects `match`/`switch` arm conditions that are string/int literals mirroring an existing backed enum's case values.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_unnamed-vocabulary-literal": {
            "title": "UnnamedVocabularyLiteral",
            "help": "A raw string filling an argument the codebase ELSEWHERE fills from a named vocabulary — a `$this->expect('{')` beside a `$this->expect(Token::COLON)`, when `Token::BRACE_OPEN` already holds it.",
            "type": "flag",
            "default": "true",
            "group": "backend/enums-with-behaviour",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_generic-exception": {
            "title": "GenericException",
            "help": "Throwing a generic SPL/base exception (`throw new \\RuntimeException(...)`) instead of a named domain exception.",
            "type": "flag",
            "default": "true",
            "group": "backend/exceptions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_message-at-throw": {
            "title": "MessageAtThrow",
            "help": "`throw new X(\"…message…\")` — the failure described with a prose string at the throw site instead of a named factory carrying domain VALUES (`throw OrderNotFound::forId($id)`).",
            "type": "flag",
            "default": "true",
            "group": "backend/exceptions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_swallow-catch": {
            "title": "SwallowCatch",
            "help": "A `catch` that swallows the failure into absence — an empty body, or whose only effect is `return null/false/[]`.",
            "type": "flag",
            "default": "true",
            "group": "backend/exceptions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_wrapping-without-cause": {
            "title": "WrappingWithoutCause",
            "help": "Throwing a new exception inside a `catch` without passing the caught one on as its cause (`previous`) — the original failure and its stack trace are dropped, so the wrapped error lies about where it came from.",
            "type": "flag",
            "default": "true",
            "group": "backend/exceptions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_constructor-side-effect": {
            "title": "ConstructorSideEffect",
            "help": "A constructor with a SIDE EFFECT — it calls a collaborator and throws the result away, so the call was made for what it DID: eager-loading a relation, writing a global default, narrowing a query builder the caller still holds.",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_divergent-twin": {
            "title": "DivergentTwin",
            "help": "Flags a method that does the same job as another and does STRICTLY LESS of it.",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-function": {
            "title": "DuplicateFunction",
            "help": "Two-or-more functions/methods with an identical AST — the same code copy-pasted, down to a formatting-blind structural hash (spacing, newlines, and comments are ignored; only real code differences count).",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_manufactured-fake-fill": {
            "title": "ManufacturedFakeFill",
            "help": "Detects filling an argument with a manufactured fake (empty string, zero, false) on absence; real defaults like `?? 'EUR'` are legitimate.",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_mutable-static-state": {
            "title": "MutableStaticState",
            "help": "A write to static state — a global with a namespace in front of it.",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_near-duplicate-function": {
            "title": "NearDuplicateFunction",
            "help": "Two-or-more functions/methods with the same SHAPE but not identical text — the same control-flow skeleton differing only in variable names or literal values (a type-2 clone).",
            "type": "flag",
            "default": "true",
            "group": "backend/fix-at-the-source",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_coalesced-loop-subject": {
            "title": "CoalescedLoopSubject",
            "help": "A `foreach` that decides in its own header whether it was HANDED anything to iterate — `foreach ($below[$id] ?? [] as $child)`, `$below` being a parameter.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_deep-nesting": {
            "title": "DeepNesting",
            "help": "An `if` nested three-deep — a pyramid of conditions.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_if-else-ladder": {
            "title": "IfElseLadder",
            "help": "An `if`/`elseif` ladder of four-plus branches — a chain of conditions doing the job of a `match`, a method on the type, or polymorphic dispatch.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_inline-throw": {
            "title": "InlineThrow",
            "help": "Detects `?? throw` buried in a call arg or dereference, not bare return statements.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_loop-inverted-guard": {
            "title": "LoopInvertedGuard",
            "help": "A loop whose entire body is wrapped in one `if` — the iteration's real work pushed a level deep behind a condition.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_nested-ternary": {
            "title": "NestedTernary",
            "help": "A nested / chained ternary — `$a ? $b : ($c ? $d : $e)` — folds a branching decision into one unreadable expression where the operator precedence is a trap.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_non-counting-for": {
            "title": "NonCountingFor",
            "help": "A `for` that is not counting — its step ASSIGNS the next thing rather than advancing a counter, as in `for ($one = $r; $one !== null; $one = $one instanceof Traceable ? $one->getAbove() : null)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_redundant-else": {
            "title": "RedundantElse",
            "help": "An `else` after an `if` branch that already exits (`return`/`throw`/`continue`/ `break`).",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_short-circuit-statement": {
            "title": "ShortCircuitStatement",
            "help": "A short-circuit operator standing as a whole statement — `$node->isBuilt() && $node->built()->forget();`.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_ternary-statement": {
            "title": "TernaryStatement",
            "help": "A ternary standing as a whole statement — `$this->holds($id) ? array_push(…) : $gone[] = $id;`.",
            "type": "flag",
            "default": "true",
            "group": "backend/guard-clauses-and-flow",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_config-read": {
            "title": "ConfigRead",
            "help": "Reading configuration with `config(...)` inside a class instead of injecting a typed config object.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_container-reach": {
            "title": "ContainerReach",
            "help": "Reaching into the container with `app()`/`resolve()` from a class the container resolves — the dependency belongs in the constructor.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_dead-config-key": {
            "title": "DeadConfigKey",
            "help": "A key declared in `config/*.php` that nothing in the codebase names.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_dead-event-wiring": {
            "title": "DeadEventWiring",
            "help": "An `Event::listen(X::class, …)` whose event class nothing can fire: X is never constructed and never dispatched statically.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_duplicated-config-default": {
            "title": "DuplicatedConfigDefault",
            "help": "A read of a config key that supplies its OWN fallback while the config file already states one — `intOr('shop.realtime.port', 8086)` against `'port' => env('PORT', 8086)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_facade-call": {
            "title": "FacadeCall",
            "help": "Detects Laravel facade calls that hide dependencies; inject the underlying contract instead.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_mass-update-at-call-site": {
            "title": "MassUpdateAtCallSite",
            "help": "A mass `->update([...])` on an Eloquent model at a call site — an untyped array of attributes smuggling a mutation past the model's own methods.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_model-mutation-at-call-site": {
            "title": "ModelMutationAtCallSite",
            "help": "Setting an Eloquent model's properties at a call site and then `->save()`-ing it — the mutation belongs behind an intention-revealing method on the model (`$order->markPaid()`), not smeared across the caller.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_orphaned-binding": {
            "title": "OrphanedBinding",
            "help": "A `bind`/`singleton`/`scoped`/`instance` registration whose abstract nothing outside the registration ever names — no type-hint, no `app()`/`make()`, no mention at all.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_raw-request-input": {
            "title": "RawRequestInput",
            "help": "An untyped read off a Laravel request — `->input(...)`, `->get(...)`, `->query(...)`, `->post(...)` on a `Request`/`FormRequest`/MCP request.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_request-accessor-recast": {
            "title": "RequestAccessorRecast",
            "help": "A typed request accessor immediately re-flattened to a bare string — `$request->string($k)->toString()` or `(string) $request->string($k)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/laravel-idioms",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_bare-state-predicate": {
            "title": "BareStatePredicate",
            "help": "A `bool` about the object's OWN state, named as a bare verb — `binds()` where `isBound()` belongs.",
            "type": "flag",
            "default": "true",
            "group": "backend/method-mood",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_narrated-command": {
            "title": "NarratedCommand",
            "help": "A command whose name narrates instead of ordering — `hides()` where `hide()` belongs.",
            "type": "flag",
            "default": "true",
            "group": "backend/method-mood",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_constructor-orchestration": {
            "title": "ConstructorOrchestration",
            "help": "A page object imperatively filling a public slot in its constructor.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_injected-service-not-hidden": {
            "title": "InjectedServiceNotHidden",
            "help": "Detects injected services in public properties without #[Hidden] on page objects; non-public properties are exempt.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_manual-output-transform": {
            "title": "ManualOutputTransform",
            "help": "Flags a `Data` slot that hand-flattens one value object into a wire array — a getter hook, a `#[Computed]` method, or a constructor assignment — where a `#[WithTransformer]` should own the shape.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_page-object-missing-typescript": {
            "title": "PageObjectMissingTypeScript",
            "help": "Flags a page object — a `Data` that composes multiple nested `Data` AND travels back in a response — carrying no `#[TypeScript]`.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_service-location-in-page-object": {
            "title": "ServiceLocationInPageObject",
            "help": "Detects page objects using service location (`app()`, `resolve()`) instead of injecting via `#[FromContainer]` attributes.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_transformer-without-ts-type": {
            "title": "TransformerWithoutTsType",
            "help": "Flags a custom `#[WithTransformer]` on a `Data` property with no paired `#[TypeScriptType]` / `#[LiteralTypeScriptType]`, so the generated TS keeps the PHP type while the wire carries the transformed shape.",
            "type": "flag",
            "default": "true",
            "group": "backend/page-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_computed-boolean-argument": {
            "title": "ComputedBooleanArgument",
            "help": "A chooser taking only bool(s) that every caller computes off the SAME object — `CornerInset::of($editor->inZenMode() || $editor->hasPanelOpen())`.",
            "type": "flag",
            "default": "true",
            "group": "backend/pass-the-object",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_converted-argument": {
            "title": "ConvertedArgument",
            "help": "Flags a parameter declared in the wrong currency: the caller holds a value, converts it into the form the callee wants, and passes the conversion — `Raises::of(ClassAlias::of($interaction), …)`.",
            "type": "flag",
            "default": "true",
            "group": "backend/pass-the-object",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_derived-argument": {
            "title": "DerivedArgument",
            "help": "Flags a call site that hands over a PROJECTION of a value rather than the value itself, where the callee could have derived it — `persist($request, $request->getShopChannelId())` wanted only `$request`.",
            "type": "flag",
            "default": "true",
            "group": "backend/pass-the-object",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_param-resolved-from-param": {
            "title": "ParamResolvedFromParam",
            "help": "Detects methods unpacking a target from a container parameter; pass the resolved object, not the container plus a key.",
            "type": "flag",
            "default": "true",
            "group": "backend/pass-the-object",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_repeated-guard": {
            "title": "RepeatedGuard",
            "help": "Flags a compound boolean guard (≥2 substantive conjuncts, at least one NOT a bare `instanceof` — pure-type chains are the repeated-type-guard's domain) that recurs in ≥2 places.",
            "type": "flag",
            "default": "true",
            "group": "backend/repeated-call-helper",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_repeated-named-call": {
            "title": "RepeatedNamedCall",
            "help": "Flags a `with`-style (variadic-parameter) method — `copyWith(mixed ...$changes)` — called with the SAME named argument, carrying construction boilerplate, at $threshold+ sites that resolve to the SAME method (grouped by its declaring class, so inherited calls on subclasses of one base collapse into one group and unrelated classes stay separate).",
            "type": "flag",
            "default": "true",
            "group": "backend/repeated-call-helper",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_repeated-type-guard": {
            "title": "RepeatedTypeGuard",
            "help": "Flags a multi-`instanceof` type-narrowing guard (`$x instanceof A && $x->y instanceof B`) that recurs VERBATIM in ≥2 places — a check copied instead of named.",
            "type": "flag",
            "default": "true",
            "group": "backend/repeated-call-helper",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_nullable-registry-lookup": {
            "title": "NullableRegistryLookup",
            "help": "Detects registries returning null on miss (`$this->items[$key] ?? null`) instead of throwing.",
            "type": "flag",
            "default": "true",
            "group": "backend/role-vocabulary",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_boundary-duplicated-operation": {
            "title": "BoundaryDuplicatedOperation",
            "help": "Two entry points on DIFFERENT kinds of boundary — a console command and an MCP tool, a controller and a command — performing the same domain calls.",
            "type": "flag",
            "default": "true",
            "group": "backend/route-actions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_dangling-route-name": {
            "title": "DanglingRouteName",
            "help": "A `route('x')` / `to_route('x')` lookup whose name no registration mints.",
            "type": "flag",
            "default": "true",
            "group": "backend/route-actions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-route": {
            "title": "DuplicateRoute",
            "help": "Detects two-or-more routes with the same verb+action bound to different URLs.",
            "type": "flag",
            "default": "true",
            "group": "backend/route-actions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-route-action": {
            "title": "DuplicateRouteAction",
            "help": "Detects duplicate thin delegations to the same service across ≥2 controllers.",
            "type": "flag",
            "default": "true",
            "group": "backend/route-actions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_route-delegates-to-controller": {
            "title": "RouteDelegatesToController",
            "help": "Detects a route action that thin-wraps another route action — a controller pass-through that creates redundant HTTP entry points.",
            "type": "flag",
            "default": "true",
            "group": "backend/route-actions",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_all-nullable-data": {
            "title": "AllNullableData",
            "help": "Detects Spatie Data classes where every field is nullable; make required fields non-nullable so `from()` fails on a real miss.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_all-optional-data": {
            "title": "AllOptionalData",
            "help": "Detects a `Data` class where EVERY promoted property is `T|Optional` — the all-optional sibling of the all-nullable \"god\" DTO.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_data-collection-type": {
            "title": "DataCollectionType",
            "help": "Flags a `Data` property typed as `DataCollection` — it must be `array` (preferred) or `Collection` with `#[DataCollectionOf(X)]`.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_data-method-hint-collision": {
            "title": "DataMethodHintCollision",
            "help": "A Spatie `Data` class with a `@method` docblock tag that re-declares a method the class ACTUALLY has, colliding with it (`@method static static fromCredential(...)` over a real `fromCredential()`).",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_hook-missing-computed": {
            "title": "HookMissingComputed",
            "help": "Flags a get-only property hook on a `Data` class that lacks `#[Computed]`.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_manual-hydration-loop": {
            "title": "ManualHydrationLoop",
            "help": "`<Data>::from(...)` called per item of a collection — inside a `foreach`/`for`/ `while` loop, or as an `array_map` callback.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_manual-input-cast": {
            "title": "ManualInputCast",
            "help": "Flags a `Data` value-object property that is hand-built at EVERY `::from()` site (via the whole-program DataConstructions index), where a `#[WithCast]` / `Castable` should own the mapping once.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_nested-type-missing-typescript": {
            "title": "NestedTypeMissingTypeScript",
            "help": "Flags a property on a `#[TypeScript]` Data whose nested Data/backed-enum type itself lacks `#[TypeScript]` — the transformer emits it as `undefined`, a silent hole in the generated frontend contract.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_new-data-object": {
            "title": "NewDataObject",
            "help": "Detects `new Data(...)` instead of `::from(...)` — skips casts, name maps, nested hydration, and factories.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_non-final-data": {
            "title": "NonFinalData",
            "help": "Detects non-final Spatie Data classes (DTOs are values, not bases).",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_null-to-optional-map": {
            "title": "NullToOptionalMap",
            "help": "Flags a hand-rolled null→`Optional` map — an \"absent\" Optional (`new Optional` OR the preferred `Optional::create()`) sitting as a ternary fallback (`$x === null ? Optional::create() : Foo::from($x)`) or the right of a `??` (`expr() ?? Optional::create()`).",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_nullable-wire-object": {
            "title": "NullableWireObject",
            "help": "Flags a nullable nested-object field (`T | null`, T a `Data` subclass or enum) on a `#[TypeScript]` Data class, where `T | Optional` belongs: on the wire `?T = null` ships `\"field\": null`, whereas `Optional` omits it — what the frontend's optional-chaining reads for \"absent\".",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_prefer-optional-create": {
            "title": "PreferOptionalCreate",
            "help": "Flags a raw `new Optional` in a runtime expression and prefers Spatie's built-in `Optional::create()` factory.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_data-to-array-roundtrip": {
            "title": "DataToArrayRoundtrip",
            "help": "Flags `X::from(...)->toArray()` whose array result sits in a `::from` slot typed `X` (a nested `Data` or a `#[DataCollectionOf(X)]` element) that re-hydrates it — a build → array → build round-trip.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_derived-collection-cast": {
            "title": "DerivedCollectionCast",
            "help": "Flags `array_map(E::for(...), $xs)` filling a `#[DataCollectionOf(E)]` slot — a per-item DERIVATION the call site does by hand, where a `#[WithCast]`/`IterableItemCast` on the property should own it.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_hand-key-remap": {
            "title": "HandKeyRemap",
            "help": "Flags `SomeData::from(['camelKey' => $src['snake_key'], …])` — a hand-written snake→camel key translation off one source array that a class-level `#[MapInputName(SnakeCaseMapper::class)]` + `::from($src)` owns once.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_redundant-enum-unwrap": {
            "title": "RedundantEnumUnwrap",
            "help": "Flags an enum destructured to `->value` inside a `Data::from([...])` slot whose property is typed as that same enum (`'status' => $order->status->value`).",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_redundant-native-cast": {
            "title": "RedundantNativeCast",
            "help": "Flags `Enum::from($x)` / `new DateTime($x)` / `Carbon::parse($x)` sitting in a `Data::from([...])` slot typed as that enum or a `DateTimeInterface` — the value Spatie auto-casts from the raw scalar, so the construction is ceremony.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_redundant-nested-from": {
            "title": "RedundantNestedFrom",
            "help": "Flags `X::from([array literal])` sitting in a parent `SomeData::from([...])` where the destination slot — a nested `Data` property or a `#[DataCollectionOf(X)]` element — auto-hydrates the array itself, so the wrapper is ceremony.",
            "type": "flag",
            "default": "true",
            "group": "backend/spatie-data-hydration",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_feature-envy": {
            "title": "FeatureEnvy",
            "help": "Exiled behaviour (feature envy) — a method that reaches THROUGH one other owned object's structure, iterating its collection, to do work that belongs ON that object (`$node->edges()`, not `EdgeDetector::detect($node)`).",
            "type": "flag",
            "default": "true",
            "group": "backend/tell-dont-ask",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_keyed-lookup-envy": {
            "title": "KeyedLookupEnvy",
            "help": "Feature envy through an indirect lookup — a method that uses an owned object's identity as a KEY to fetch data about it through a collaborator, then reads a fact back (`$this->registry->get($node->key)->reservedOutputNames`).",
            "type": "flag",
            "default": "true",
            "group": "backend/tell-dont-ask",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_type-switch": {
            "title": "TypeSwitch",
            "help": "A run of `instanceof` tests on the same subject, each deciding a different branch — the caller asking a value what it IS so it can decide what to do, when the value could simply be told.",
            "type": "flag",
            "default": "true",
            "group": "backend/tell-dont-ask",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_assembled-template": {
            "title": "AssembledTemplate",
            "help": "Detects a fixed multi-line template built as an array of line fragments and joined with a newline.",
            "type": "flag",
            "default": "true",
            "group": "backend/templates",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_masked-invariant": {
            "title": "MaskedInvariant",
            "help": "`$this->scratch?->call() ?? false` — defaulting a reach into the object's own TRANSIENT nullable state.",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_phantom-nullable": {
            "title": "PhantomNullable",
            "help": "A field typed `?T` whose value is always consumed as present (never guarded) per the ValueFlow provenance graph.",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_placeholder-filled-data": {
            "title": "PlaceholderFilledData",
            "help": "A Data construction handing `''` to a slot typed as a required, non-nullable `string` — the envelope's TYPES stay honest while its VALUES are manufactured.",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_redundant-arrow-return-type": {
            "title": "RedundantArrowReturnType",
            "help": "An arrow function carrying a return type its own expression already proves — `fn (): string => $this->name` where `$name` IS a `string`.",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_scratch-state-restore": {
            "title": "ScratchStateRestore",
            "help": "A method that SAVES one of its own properties to a local and RESTORES it afterwards — `$prev = $this->scope; … $this->scope = $prev;`.",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_useless-property-hook": {
            "title": "UselessPropertyHook",
            "help": "A `get` hook whose body references no `$this` (and no `parent::`) — it computes nothing from the object, so the property is stored, not derived; the hook syntax is a lie usually copied from an interface's `{ get; }` (which a plain property satisfies).",
            "type": "flag",
            "default": "true",
            "group": "backend/type-honesty",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_array-bag": {
            "title": "ArrayBag",
            "help": "An `array` read by string-literal keys — a structured bag that should be a typed value object.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_array-return-bag": {
            "title": "ArrayReturnBag",
            "help": "Detects multi-field string-keyed array returns (bags for value objects).",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_coupled-fields": {
            "title": "CoupledFields",
            "help": "A class's own VALUE fields that are really ONE object.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_data-clump": {
            "title": "DataClump",
            "help": "The same three-or-more value parameters (`string $shopId, string $userId, string $channelId`) threaded through two-or-more signatures in different classes.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_flat-field-cluster": {
            "title": "FlatFieldCluster",
            "help": "Flags a `#[TypeScript]` `Data` that spreads a value object flat across sibling scalar fields sharing a camelCase prefix — `wireType`/`wireLabel` that should be `wire: Wire{type, label}` — WHEN a small value object named for that prefix, whose fields ARE the flat remainders, already exists.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_hand-rolled-wither": {
            "title": "HandRolledWither",
            "help": "A wither that rebuilds its object by re-spelling the WHOLE constructor field list (`return new self($this->a, $this->b, $changed, $this->d);`) instead of saying only what changes.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_mutable-value-object": {
            "title": "MutableValueObject",
            "help": "A VALUE that can change after it is built.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_positional-tuple-return": {
            "title": "PositionalTupleReturn",
            "help": "Detects positional tuples — keyless arrays without types or names that callers must destructure by position.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_raw-decoded-array-return": {
            "title": "RawDecodedArrayReturn",
            "help": "Detects raw `json_decode(...)` arrays returned from boundaries.",
            "type": "flag",
            "default": "true",
            "group": "backend/value-objects",
            "when": [
                {
                    "language_php": true
                }
            ],
            "detail": true
        },
        "sin_csharp-blank-string-default": {
            "title": "BlankStringDefault",
            "help": "A `string` defaulted to `\"\"` and then asked whether it is blank — absence spelled as a blank in a type that says the value is always there.",
            "type": "flag",
            "default": "true",
            "group": "csharp/absence",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-cancelled-coalesce": {
            "title": "CancelledCoalesce",
            "help": "A fallback compared against the value it falls back to — the C# twin of the PHP cancelled-coalesce and the Python cancelled-fallback rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/absence",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-invented-default": {
            "title": "InventedDefault",
            "help": "A missing value papered over with an invented `\"\"`, `0` or `false` — the C# twin of Python's invented-default rule, in its hardened shape.",
            "type": "flag",
            "default": "true",
            "group": "csharp/absence",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-null-forgiven": {
            "title": "NullForgiven",
            "help": "The null-forgiving `!` applied to what is declared nullable — a `T?` field, property, local, parameter or return, as the compiler resolves it.",
            "type": "flag",
            "default": "true",
            "group": "csharp/absence",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-flag-argument": {
            "title": "FlagArgument",
            "help": "A method whose whole body is a two-way branch on one of its `bool` parameters — the C# twin of the PHP and Python flag-argument rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/behaviour-per-method",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-member-after-method": {
            "title": "MemberAfterMethod",
            "help": "State declared below a constructor or a method — the C# twin of the PHP and Python member-after-method rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/class-layout",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-member-out-of-order": {
            "title": "MemberOutOfOrder",
            "help": "A constant declared below a type's per-object state — the C# twin of the PHP and Python member-out-of-order rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/class-layout",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-namespace-cycle": {
            "title": "NamespaceCycle",
            "help": "Two of the project's namespaces that each use the other, found on the thinner direction — the twin of the backend's and Python's namespace-cycle rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/dependency-direction",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-namespace-dependency": {
            "title": "NamespaceDependency",
            "help": "A reference out of a declared layer into a namespace it did not declare it may use — the twin of the backend's and Python's namespace-dependency rules, over the namespaces the compiler resolved.",
            "type": "flag",
            "default": "true",
            "group": "csharp/dependency-direction",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-archaeology-comment": {
            "title": "ArchaeologyComment",
            "help": "A comment narrating the code's history — the C# twin of the PHP and Python archaeology-comment rules, read by the one shared reading of prose, narratesHistory.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-bloated-docblock": {
            "title": "BloatedDocblock",
            "help": "A type's doc comment running to two or more paragraphs — the C# twin of the PHP and Python bloated-docblock rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-ceremony-docblock": {
            "title": "CeremonyDocblock",
            "help": "A doc comment that only repeats the signature it sits on — the C# twin of the PHP and Python ceremony-docblock rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-dangling-doc-reference": {
            "title": "DanglingDocReference",
            "help": "A doc comment naming, in a `cref`, a type or member the project does not declare — the C# twin of the PHP and Python dangling-doc-reference rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-negative-space-comment": {
            "title": "NegativeSpaceComment",
            "help": "A comment defending the code against a strawman — the C# twin of the PHP and Python negative-space-comment rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-restated-comment": {
            "title": "RestatedComment",
            "help": "A comment narrating the statement below it — every word of it already spelled by the code.",
            "type": "flag",
            "default": "true",
            "group": "csharp/documentation",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-csharp-method": {
            "title": "DuplicateMethod",
            "help": "Two-or-more C# members with one body — methods, accessors and local functions alike — compared by a formatting-blind fingerprint of the body alone, so the copy that was renamed is caught with the one that was not.",
            "type": "flag",
            "default": "true",
            "group": "csharp/duplication",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_near-duplicate-csharp-method": {
            "title": "NearDuplicateMethod",
            "help": "Two-or-more C# members with one control-flow skeleton that differ only in their local names or their string, number and character literals — a type-2 clone, one method waiting for a parameter.",
            "type": "flag",
            "default": "true",
            "group": "csharp/duplication",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-const-class-enum": {
            "title": "ConstClassEnum",
            "help": "A class of nothing but `const` strings or numbers, one of which is compared as a case somewhere — the C# twin of the PHP const-class-enum rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/enums",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-enum-case-or-chain": {
            "title": "EnumCaseOrChain",
            "help": "Two or more cases of one enum the codebase declares, tested together by `||` or an `is … or …` pattern — the C# twin of the PHP enum-case-or-chain rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/enums",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-in-array-mirrors-enum": {
            "title": "InArrayMirrorsEnum",
            "help": "A membership test against strings written right there — `Contains` over an inline list, or an `is … or …` pattern — that all name members of one enum the codebase declares: the C# twin of the PHP in-array-mirrors-enum rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/enums",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-match-default-returns-null": {
            "title": "MatchDefaultReturnsNull",
            "help": "A switch over an enum the codebase declares that names every member, its fallback answering nothing — the C# twin of the PHP match-default-returns-null rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/enums",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-string-mirrors-enum": {
            "title": "StringMirrorsEnum",
            "help": "A `switch` or an `if` ladder dispatching on two or more strings that all name members of one enum the codebase declares — the C# twin of the backend's string-match-mirrors-enum rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/enums",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-generic-throw": {
            "title": "GenericThrow",
            "help": "An exception that names no failure thrown with its description written at the throw — the C# twin of the backend's generic-exception and message-at-throw rules and Python's message-string raise.",
            "type": "flag",
            "default": "true",
            "group": "csharp/exceptions",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-swallowed-exception": {
            "title": "SwallowedException",
            "help": "A `catch` that catches everything and makes it vanish — the C# twin of the backend's and Python's swallowed-exception rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/exceptions",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-wrapping-without-cause": {
            "title": "WrappingWithoutCause",
            "help": "A new exception thrown from a catch without the caught one as its inner exception — the C# twin of the PHP and Python wrapping-without-cause rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/exceptions",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-constructor-side-effect": {
            "title": "ConstructorSideEffect",
            "help": "A constructor that tells a collaborator it was handed to act and ignores the answer — the C# twin of the Python constructor-side-effect rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/fix-at-the-source",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-mutable-static-state": {
            "title": "MutableStaticState",
            "help": "A static field written from a method — the C# twin of the Python mutable-static-state rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/fix-at-the-source",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-coalesced-loop-subject": {
            "title": "CoalescedLoopSubject",
            "help": "A loop over a collection defaulted to empty in its own header — the C# twin of the PHP and Python coalesced-loop-subject rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-inline-throw": {
            "title": "InlineThrow",
            "help": "A `?? throw` buried in a call's argument or receiver — the C# twin of the PHP inline-throw rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-loop-wrapped-in-if": {
            "title": "LoopWrappedInIf",
            "help": "A loop whose whole body is one `if` around real work — the C# twin of the backend's and Python's loop-wrapped-in-if rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-nested-ternary": {
            "title": "NestedTernary",
            "help": "A conditional expression nested in another's branch — the C# twin of the PHP nested-ternary rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-non-counting-for": {
            "title": "NonCountingFor",
            "help": "A `for` whose step assigns the next item rather than moving a counter — the C# twin of the PHP non-counting-for rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-subject-ladder": {
            "title": "SubjectLadder",
            "help": "An `if`/`else if` ladder comparing one subject with constant after constant — the C# twin of the backend's and Python's ladder rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_deep-csharp-nesting": {
            "title": "DeepNesting",
            "help": "An `if`, loop or `switch` that opens a fourth level of choices inside one C# method — the arrow the backend's and Python's nesting rules find, counted over every kind of choice C# writes.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_redundant-csharp-else": {
            "title": "RedundantElse",
            "help": "An `else` after an `if` branch that already left — the twin of the backend's and Python's redundant-else rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/flow",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-bare-state-predicate": {
            "title": "BareStatePredicate",
            "help": "A `bool` about the object alone, named as a third-person claim — the C# twin of the PHP and Python bare-state-predicate rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/method-mood",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-repeated-guard": {
            "title": "RepeatedGuard",
            "help": "The same compound condition about an object's data asked at two or more sites — the C# twin of the PHP and Python repeated-guard rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/repeated-call-helper",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-repeated-named-call": {
            "title": "RepeatedNamedCall",
            "help": "The same `with` copy of one record — the same members set to the same constants — at two or more sites: the C# twin of the PHP and Python repeated-named-call rules, whose copy-with-changes call C# writes as `with`.",
            "type": "flag",
            "default": "true",
            "group": "csharp/repeated-call-helper",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-repeated-type-guard": {
            "title": "RepeatedTypeGuard",
            "help": "The same chain of two or more type checks at two or more sites — the C# twin of the PHP and Python repeated-type-guard rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/repeated-call-helper",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-type-switch": {
            "title": "TypeSwitch",
            "help": "A switch whose cases ask which of two or more of the codebase's own types a value is — the C# twin of the PHP and Python type-switch rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/tell-dont-ask",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-assembled-template": {
            "title": "AssembledTemplate",
            "help": "Three or more lines written right there, two of them fixed text, joined with a newline — or written one `AppendLine` at a time on one builder, C#'s own way of taking a template apart: the C# twin of the PHP and Python assembled-template rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/templates",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-placeholder-filled-data": {
            "title": "PlaceholderFilledData",
            "help": "A record the codebase declares, built with a blank string in a required `string` slot — the C# twin of the PHP and Python placeholder-filled-data rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/type-honesty",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-array-return-bag": {
            "title": "ArrayReturnBag",
            "help": "A dictionary built with two or more fixed string keys, handed back by a member — the C# twin of the PHP array-return-bag rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/value-objects",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-data-clump": {
            "title": "DataClump",
            "help": "The same three or more value parameters — by type and name — declared by members of two or more types: one clump of data threaded through the code instead of named.",
            "type": "flag",
            "default": "true",
            "group": "csharp/value-objects",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-dictionary-bag": {
            "title": "DictionaryBag",
            "help": "A string-keyed dictionary or JSON object read by keys written in the source — the C# twin of Python's dict-bag rule, and like it, found both where the key is read and where a lookup helper is handed the key — a dictionary the reading code owns, a parameter or a local.",
            "type": "flag",
            "default": "true",
            "group": "csharp/value-objects",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-mutable-value-object": {
            "title": "MutableValueObject",
            "help": "A record that changes after construction — a `set` accessor on it, or a write to its own state outside a constructor — the C# twin of the PHP mutable-value-object rule.",
            "type": "flag",
            "default": "true",
            "group": "csharp/value-objects",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_csharp-positional-tuple-return": {
            "title": "PositionalTupleReturn",
            "help": "A member whose declared result is a tuple with unnamed slots — the C# twin of the PHP and Python positional-tuple-return rules.",
            "type": "flag",
            "default": "true",
            "group": "csharp/value-objects",
            "when": [
                {
                    "language_cs": true
                }
            ],
            "detail": true
        },
        "sin_mirrored-server-type": {
            "title": "MirroredServerType",
            "help": "Detects hand-written TypeScript types mirroring backend Spatie Data classes; the server should own the type and the frontend generate from it.",
            "type": "flag",
            "default": "true",
            "group": "frontend/mirrored-server-type",
            "when": [
                {
                    "language_vue": true
                },
                {
                    "language_ts": true
                }
            ],
            "detail": true
        },
        "sin_compound-inline-component": {
            "title": "CompoundInlineComponent",
            "help": "Detects compound UI primitives assembled inline (component root with ≥2 descendants sharing a prefix tag).",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_deep-data-reach": {
            "title": "DeepDataReach",
            "help": "Detects clusters of deep data reaches (e.g., `order.customer.name`, `order.customer.email`) sharing one nested object.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_deep-nested": {
            "title": "DeepNested",
            "help": "A template nested far too deep; identifies the natural extraction boundary by climbing from the deep element up the single-child wrapper stack for a coherent unit.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-element": {
            "title": "DuplicateElement",
            "help": "Detects identical template blocks (structural comparison, blind to formatting); only substantial blocks and the largest duplicate are flagged.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_near-duplicate-element": {
            "title": "NearDuplicateElement",
            "help": "Template blocks with one SKELETON that bind different data — the same tags, attribute names and nesting, with the values and text left out — within a template, across components, or as two components' whole templates.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_prop-drilling": {
            "title": "PropDrilling",
            "help": "Detects a prop threaded through ≥2 components unused.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_prop-mutation": {
            "title": "PropMutation",
            "help": "Detects component writing its own props (v-model or assignment); only bare prop writes are flagged, not shadowed locals.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-components",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_control-flow-on-element": {
            "title": "ControlFlowOnElement",
            "help": "Detects structural directives (`v-if`/`v-else-if`/`v-else`/`v-for`) on real elements instead of `<template>` wrappers — mixes structure (what renders) with content.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-control-flow",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_index-as-key": {
            "title": "IndexAsKey",
            "help": "Detects `v-for` keyed by numeric index (`:key=\"index\"`), which shifts when items insert/reorder, breaking Vue patching and state.",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-control-flow",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_loop-with-condition": {
            "title": "LoopWithCondition",
            "help": "Detects `v-for` and `v-if`/`v-else-if` on the same element (Vue prioritizes if over for, causing correctness/performance issues).",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-control-flow",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_switch-case": {
            "title": "SwitchCase",
            "help": "Detects `v-if`/`v-else-if` chains testing the same value against different cases (switch as conditionals).",
            "type": "flag",
            "default": "true",
            "group": "frontend/vue-control-flow",
            "when": [
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_python-blank-string-default": {
            "title": "BlankStringDefault",
            "help": "A `str` parameter or field defaulting to the blank whose own scope then asks whether it is blank — the twin of the backend's BlankStringDefaultDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/absence",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-cancelled-fallback": {
            "title": "CancelledFallback",
            "help": "A default compared against itself — the twin of the backend's CancelledCoalesceDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/absence",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-conditional-spread": {
            "title": "ConditionalSpread",
            "help": "A conditional spread into an empty collection — the twin of the backend's ConditionalArraySpreadDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/absence",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-invented-default": {
            "title": "InventedDefault",
            "help": "`f(x or \"\")` — an empty scalar invented to fill an argument on absence.",
            "type": "flag",
            "default": "true",
            "group": "python/absence",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-nullable-callback": {
            "title": "NullableCallback",
            "help": "A function taking a `None`-defaulted callable it then asks about — the twin of the backend's NullableCallbackDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/absence",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-flag-argument": {
            "title": "FlagArgument",
            "help": "A function that is nothing but a choice on one of its own parameters — the twin of the backend's FlagArgumentDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/behaviour-per-method",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-member-after-method": {
            "title": "MemberAfterMethod",
            "help": "Class-level state written below a method — the twin of the backend's MemberAfterMethodDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/class-layout",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-member-out-of-order": {
            "title": "MemberOutOfOrder",
            "help": "A constant arriving below a field in a class's head — the twin of the backend's MemberOutOfOrderDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/class-layout",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-namespace-cycle": {
            "title": "NamespaceCycle",
            "help": "Two packages importing each other — the twin of the backend's NamespaceCycleDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/dependency-direction",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-namespace-dependency": {
            "title": "NamespaceDependency",
            "help": "An import out of a declared layer into a package it may not use — the twin of the backend's NamespaceDependencyDetector, over dotted package names.",
            "type": "flag",
            "default": "true",
            "group": "python/dependency-direction",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-archaeology-comment": {
            "title": "ArchaeologyComment",
            "help": "A comment or docstring about the code's past — the twin of the backend's ArchaeologyCommentDetector, reading the same phrases.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-bloated-docblock": {
            "title": "BloatedDocblock",
            "help": "A class docstring that runs to an essay — the twin of the backend's BloatedDocblockDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-ceremony-docblock": {
            "title": "CeremonyDocblock",
            "help": "A docstring that only repeats the annotations — the twin of the backend's CeremonyDocblockDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-dangling-doc-reference": {
            "title": "DanglingDocReference",
            "help": "A docstring cross-reference to a first-party name nothing declares — the twin of the backend's DanglingDocReferenceDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-negative-space-comment": {
            "title": "NegativeSpaceComment",
            "help": "A comment or docstring defending the code against a strawman — the twin of the backend's NegativeSpaceCommentDetector, reading the same phrases.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-restated-comment": {
            "title": "RestatedComment",
            "help": "A comment that says nothing its statement does not — the twin of the backend's RestatedCommentDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/documentation",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-python-function": {
            "title": "DuplicateFunction",
            "help": "Two-or-more Python functions with one body — module functions and methods alike — compared by a formatting-blind fingerprint of the body alone, docstrings aside, so the copy that was renamed is caught with the one that was not.",
            "type": "flag",
            "default": "true",
            "group": "python/duplication",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_near-duplicate-python-function": {
            "title": "NearDuplicateFunction",
            "help": "Two-or-more Python functions with one control-flow skeleton that differ only in their local names or their string/number literals — a type-2 clone, one function waiting for a parameter.",
            "type": "flag",
            "default": "true",
            "group": "python/duplication",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-constant-class-enum": {
            "title": "ConstantClassEnum",
            "help": "A class of nothing but scalar constants — the twin of the backend's ConstClassEnumDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-enum-case-or-chain": {
            "title": "EnumCaseOrChain",
            "help": "An `or` chain testing one subject against several members of one enum the codebase declares — the twin of the backend's EnumCaseOrChainDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-enum-value-match": {
            "title": "EnumValueMatch",
            "help": "A `match` over an enum's raw values at a call site — the twin of the backend's EnumValueMatchDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-in-literals-mirrors-enum": {
            "title": "InLiteralsMirrorsEnum",
            "help": "An `in` test against literals that are all one declared enum's values — the twin of the backend's InArrayMirrorsEnumDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-match-wildcard-returns-none": {
            "title": "MatchWildcardReturnsNone",
            "help": "A `match` over an enum's members whose wildcard swallows the unhandled ones into nothing — the twin of the backend's MatchDefaultReturnsNullDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-string-match-mirrors-enum": {
            "title": "StringMatchMirrorsEnum",
            "help": "A `match` on loose strings that are one declared enum's values — the twin of the backend's StringMatchMirrorsEnumDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-unnamed-vocabulary-literal": {
            "title": "UnnamedVocabularyLiteral",
            "help": "A raw string where the codebase elsewhere spells the same parameter by name — the twin of the backend's UnnamedVocabularyLiteralDetector, decided by ConstantVocabulary.",
            "type": "flag",
            "default": "true",
            "group": "python/enums",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-message-string-raise": {
            "title": "MessageStringRaise",
            "help": "A builtin that names no failure raised with its description written at the raise — the Python twin of the backend's GenericExceptionDetector and MessageAtThrowDetector together.",
            "type": "flag",
            "default": "true",
            "group": "python/exceptions",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-raise-without-cause": {
            "title": "RaiseWithoutCause",
            "help": "A new exception raised from an `except` block without `from` — the twin of the backend's WrappingWithoutCauseDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/exceptions",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-swallowed-exception": {
            "title": "SwallowedException",
            "help": "A handler that catches everything and makes it vanish — the Python twin of the backend's SwallowCatchDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/exceptions",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-constructor-side-effect": {
            "title": "ConstructorSideEffect",
            "help": "A class whose `__init__` acts on a collaborator and discards the result — the twin of the backend's ConstructorSideEffectDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/fix-at-the-source",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-divergent-twin": {
            "title": "DivergentTwin",
            "help": "A function that does the same job as another and strictly less of it — the twin of the backend's DivergentTwinDetector, read by the shared DivergentTwins over what each function reaches.",
            "type": "flag",
            "default": "true",
            "group": "python/fix-at-the-source",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-mutable-static-state": {
            "title": "MutableStaticState",
            "help": "A write to state no instance owns — the twin of the backend's MutableStaticStateDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/fix-at-the-source",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_deep-python-nesting": {
            "title": "DeepNesting",
            "help": "An `if`, loop or `match` that opens a fourth level of choices inside one function — the arrow the backend's DeepNestingDetector finds, counted over every kind of choice Python writes.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-coalesced-loop-subject": {
            "title": "CoalescedLoopSubject",
            "help": "A `for` that decides in its own header whether it was handed anything to walk — the twin of the backend's CoalescedLoopSubjectDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-conditional-statement": {
            "title": "ConditionalStatement",
            "help": "A conditional expression standing as a whole statement, its value read by nothing — the twin of the backend's TernaryStatementDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-loop-wrapped-in-if": {
            "title": "LoopWrappedInIf",
            "help": "A loop whose whole body is one `if` around real work — the twin of the backend's LoopInvertedGuardDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-nested-conditional": {
            "title": "NestedConditional",
            "help": "A conditional expression nested in another's branch, flagged once at its outermost — the twin of the backend's NestedTernaryDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-short-circuit-statement": {
            "title": "ShortCircuitStatement",
            "help": "An `and`/`or` standing as a whole statement, its value read by nothing — the twin of the backend's ShortCircuitStatementDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-subject-ladder": {
            "title": "SubjectLadder",
            "help": "An `if`/`elif` chain testing one subject against constant after constant — the Python twin of the Vue SwitchCaseDetector, at the backend ladder's length: fewer rungs are an ordinary decision, not a dispatch.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_redundant-python-else": {
            "title": "RedundantElse",
            "help": "An `else:` after an `if` branch that already left — the twin of the backend's RedundantElseDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/flow",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-bare-state-predicate": {
            "title": "BareStatePredicate",
            "help": "A `bool` about the receiver named as a bare verb — the twin of the backend's BareStatePredicateDetector, over the same verb lexicon, read in snake_case.",
            "type": "flag",
            "default": "true",
            "group": "python/method-mood",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-narrated-command": {
            "title": "NarratedCommand",
            "help": "A command named in the third person — `def hides(self) -> None` — the twin of the backend's NarratedCommandDetector, over the same verb lexicon, read in snake_case.",
            "type": "flag",
            "default": "true",
            "group": "python/method-mood",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-computed-boolean-argument": {
            "title": "ComputedBooleanArgument",
            "help": "A method of bools every caller derives from one object — the twin of the backend's ComputedBooleanArgumentDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/pass-the-object",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-converted-argument": {
            "title": "ConvertedArgument",
            "help": "A scalar parameter declared in the wrong currency — the twin of the backend's ConvertedArgumentDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/pass-the-object",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-derived-argument": {
            "title": "DerivedArgument",
            "help": "A call that hands over a projection of a value beside the value itself, or the value in pieces — the twin of the backend's DerivedArgumentDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/pass-the-object",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-param-resolved-from-param": {
            "title": "ParamResolvedFromParam",
            "help": "A function that unpacks its target from a container parameter — the twin of the backend's ParamResolvedFromParamDetector: it should take the resolved object, not the container plus a key.",
            "type": "flag",
            "default": "true",
            "group": "python/pass-the-object",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-repeated-guard": {
            "title": "RepeatedGuard",
            "help": "One compound condition written in two places — the twin of the backend's RepeatedGuardDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/repeated-call-helper",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-repeated-named-call": {
            "title": "RepeatedNamedCall",
            "help": "One `**changes` function called with the same keyword, built the same way, at site after site — the twin of the backend's RepeatedNamedCallDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/repeated-call-helper",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-repeated-type-guard": {
            "title": "RepeatedTypeGuard",
            "help": "One `isinstance` narrowing written in two places — the twin of the backend's RepeatedTypeGuardDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/repeated-call-helper",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-nullable-registry-lookup": {
            "title": "NullableRegistryLookup",
            "help": "A store that returns `None` on a miss instead of raising — the twin of the backend's NullableRegistryLookupDetector: `.get(key)` on one of the object's own dict attributes, returned as it is.",
            "type": "flag",
            "default": "true",
            "group": "python/role-vocabulary",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-feature-envy": {
            "title": "FeatureEnvy",
            "help": "Exiled behaviour — the twin of the backend's FeatureEnvyDetector, decided by FeatureEnvy.",
            "type": "flag",
            "default": "true",
            "group": "python/tell-dont-ask",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-keyed-lookup-envy": {
            "title": "KeyedLookupEnvy",
            "help": "Feature envy through a keyed lookup — the twin of the backend's KeyedLookupEnvyDetector, decided by LookupEnvy.",
            "type": "flag",
            "default": "true",
            "group": "python/tell-dont-ask",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-type-switch": {
            "title": "TypeSwitch",
            "help": "An `isinstance` ladder over the codebase's own classes — the twin of the backend's TypeSwitchDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/tell-dont-ask",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-assembled-template": {
            "title": "AssembledTemplate",
            "help": "A template assembled from line fragments — the twin of the backend's AssembledTemplateDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/templates",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-constant-property": {
            "title": "ConstantProperty",
            "help": "A `@property` that never reads the object — the twin of the backend's UselessPropertyHookDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/type-honesty",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-masked-invariant": {
            "title": "MaskedInvariant",
            "help": "A fake answer masking the object's own scratch state — the twin of the backend's MaskedInvariantDetector, decided by OwnStateMask.",
            "type": "flag",
            "default": "true",
            "group": "python/type-honesty",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-phantom-nullable": {
            "title": "PhantomNullable",
            "help": "A field typed `X | None` whose every read assumes it is there and none guards — the twin of the backend's PhantomNullableDetector, decided by the codebase's AttributeFlow.",
            "type": "flag",
            "default": "true",
            "group": "python/type-honesty",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-placeholder-filled-data": {
            "title": "PlaceholderFilledData",
            "help": "A dataclass built with `\"\"` in a field it requires as text — the twin of the backend's PlaceholderFilledDataDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/type-honesty",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-scratch-state-restore": {
            "title": "ScratchStateRestore",
            "help": "A method saving and restoring its own attribute around the call — the twin of the backend's ScratchStateRestoreDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/type-honesty",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-coupled-fields": {
            "title": "CoupledFields",
            "help": "A class whose own value fields are really one object — the twin of the backend's CoupledFieldsDetector, decided by FieldClumps.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-data-clump": {
            "title": "DataClump",
            "help": "The same three-or-more scalar parameters threaded through functions of two or more classes or modules — the Python twin of the backend's DataClumpDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-dict-bag": {
            "title": "DictBag",
            "help": "A dict-typed parameter or local read by string keys — the Python twin of the backend's ArrayBagDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-dict-return-bag": {
            "title": "DictReturnBag",
            "help": "A dict of two or more named fields handed back from a function — the twin of the backend's ArrayReturnBagDetector, with its exemptions: a spread, a nested payload, keys that name external things rather than fields, a JSON schema, a table of one class's members, the projection of one typed object, a `TypedDict` return, and a method whose contract is its base's or the language's.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-hand-rolled-replace": {
            "title": "HandRolledReplace",
            "help": "A dataclass method rebuilding its own object field by field — the twin of the backend's HandRolledWitherDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-mutable-value-object": {
            "title": "MutableValueObject",
            "help": "A dataclass value written after it is built — the twin of the backend's MutableValueObjectDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-positional-tuple-return": {
            "title": "PositionalTupleReturn",
            "help": "A tuple of different things handed back by position — the twin of the backend's PositionalTupleReturnDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_python-raw-decoded-return": {
            "title": "RawDecodedReturn",
            "help": "A raw `json.loads(...)` handed back from a boundary — the twin of the backend's RawDecodedArrayReturnDetector.",
            "type": "flag",
            "default": "true",
            "group": "python/value-objects",
            "when": [
                {
                    "language_py": true
                }
            ],
            "detail": true
        },
        "sin_defended-certain-field": {
            "title": "DefendedCertainField",
            "help": "An `?.` on one of the enclosing class's OWN fields, where that field is declared total — a guard against a case the type rules out.",
            "type": "flag",
            "default": "true",
            "group": "typescript/absence",
            "when": [
                {
                    "language_ts": true
                }
            ],
            "detail": true
        },
        "sin_falsely-optional-field": {
            "title": "FalselyOptionalField",
            "help": "A field declared optional that is INITIALISED where it is declared.",
            "type": "flag",
            "default": "true",
            "group": "typescript/absence",
            "when": [
                {
                    "language_ts": true
                }
            ],
            "detail": true
        },
        "sin_duplicate-typescript-function": {
            "title": "DuplicateFunction",
            "help": "Two-or-more TypeScript functions with one body — a `function`, a method and a `const` arrow alike, in a `.ts` module or a component's `<script>` — compared by a formatting-blind fingerprint of the body alone, so the copy that was renamed is caught with the one that was not.",
            "type": "flag",
            "default": "true",
            "group": "typescript/duplication",
            "when": [
                {
                    "language_ts": true
                },
                {
                    "language_vue": true
                }
            ],
            "detail": true
        },
        "sin_near-duplicate-typescript-function": {
            "title": "NearDuplicateFunction",
            "help": "Two-or-more TypeScript functions with one SHAPE but not one body — the same control flow, differing only in local names or string/number literals (a type-2 clone): each does the same thing to a different endpoint or key, and wants to be one function with a parameter.",
            "type": "flag",
            "default": "true",
            "group": "typescript/duplication",
            "when": [
                {
                    "language_ts": true
                },
                {
                    "language_vue": true
                }
            ],
            "detail": true
        }
    },
    "skills": "journal-skills/.agents/skills",
    "installed": "bin/commandments journal-scan",
    "services": {
        "hooks": {
            "run": "bin/commandments journal-serve",
            "restart": "always"
        }
    },
    "dashboards": [
        {
            "name": "sins",
            "title": "Sins"
        }
    ]
}

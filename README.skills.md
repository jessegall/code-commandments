# Skills

Auto-generated by `composer readme`; do not edit by hand. Part of [README.md](README.md).

_56 skills._

### Backend

| Skill | What it teaches |
|---|---|
| `Absence` | modelling a value that might be missing (`?T`, `Option`, `null`, empty, Null Object, throw). |
| `BehaviourPerMethod` | a parameter that picks WHICH behaviour runs means two methods share one name — split them and let the call site say which it wants, instead of passing a bare `true`. |
| `ClassLayout` | state at the top — traits, constants, properties and hooks above the constructor, methods after. |
| `ConcurrentState` | state shared across requests/workers (`::for($id): Concurrent<self>`). |
| `DependencyDirection` | a declared layer may only reference the layers it declared it may use — down the stack, never back up, never sideways; the direction is enforced from the project's own layer declaration. |
| `Documentation` | concise, present-tense docs; rare inline comments; never narrate the past. |
| `EnumsWithBehaviour` | a closed set of values: seal it as a native backed enum, put the per-case logic on the enum (not a `match` at every call site). |
| `Exceptions` | throwing or catching: named `::for()` factory exceptions, never swallow a failure. |
| `FixAtTheSource` | the root-cause-first move: trace a value to where it's born, never patch the symptom. Governs how every change is made. |
| `GuardClausesAndFlow` | validate preconditions at the TOP (early return/throw), flat body, happy path last; never bury a check inline. |
| `LaravelIdioms` | typed request/bag access (never raw `->input()`/`->get()`), required constructor DI (never `app()`/facade), Eloquent scopes + intention-revealing model mutation methods. |
| `MethodMood` | commands are imperatives (`hide()`), state predicates are questions (`isHidden()`). |
| `PageObjects` | the composed `Data` a controller returns for a page — container-injected + `#[Hidden]` collaborators, computed slots over a fat constructor, transformers for output shape. |
| `PassTheObject` | demand the resolved type you need, not an id plus its container: a method that takes `(Workflow $workflow, string $nodeId)` then unpacks `$workflow->graph->nodeById($nodeId)` should take the node — the caller resolves once and passes the object (and owns the not-found failure). |
| `RepeatedCallHelper` | a repeated `with`-style call passing the same named argument belongs as a named method on the receiver's type. |
| `RoleVocabulary` | a keyed store / membership set / first-match dispatcher: name it `*Registry`/`*Set`/`*Resolver`, extend the base, honour the contract. |
| `RouteActions` | route actions are thin, single entry points — no controller wrapping another, no duplicate actions, no two routes to one action. |
| `SpatieData` | how to write and construct Spatie `Data` classes — `::from()` not `new`, total types, sealed and readonly. |
| `SpatieDataHydration` | construct and consume `Data` objects without re-doing what the class declares — pass raw input, let `::from`/casts/collections build. |
| `TellDontAsk` | behaviour belongs with its data (feature envy): don't exile a loop over one object's collection into a separate class — move it onto the object (`$node->edges()`, not `EdgeDetector::detect($node)`). A Strategy over flat scalar fields is the exception. |
| `Templates` | a multi-line string is a heredoc that SHOWS its output, never a list of line fragments joined. |
| `TypeHonesty` | a type must not lie: don't fake optionality — a `?T` the design always has set, then defended with `?->`/`?? <fake>` or stashed as save/restore scratch state. Make the type certain (pass it, hold it non-nullable, a per-call value object). The complement of `absence`. |
| `ValueObjects` | give related data a type: no loose `array<string,mixed>` bags, no data clumps, no primitive obsession. (Decide the type; then `spatie-data` is how to write it.) |

### Csharp

| Skill | What it teaches |
|---|---|
| `Absence` | decide absence where the value is born — throw, return an empty collection, or a Null Object — with nullable reference types saying honestly what may be missing; never `?? ""` a required value, never `!` to silence the compiler. |
| `ClassLayout` | state at the top — constants, fields and stored properties above the constructor, methods after. |
| `Duplication` | a method body written twice becomes one shared method, parameterised by what differs. |
| `Enums` | a closed set of values is an `enum`, its per-case knowledge in one exhaustive `switch` expression beside it — not string constants compared at every call site. |
| `Exceptions` | throw named exceptions built by a static factory, never swallow a failure, and keep the cause as the inner exception. |
| `FixAtTheSource` | trace a value, an effect or a piece of state back to where it starts, and fix it there. |
| `Flow` | check preconditions at the top and leave (`return`/`throw`/`continue`), keep the body flat, no `else` after an exit, a `switch` expression or polymorphism instead of an `else if` ladder over one subject. |
| `MethodMood` | commands are orders (`Hide()`), state checks are questions (`IsHidden`, `HasParent`). |
| `TypeHonesty` | a type must not lie: no `T?` a value never is, no `!` to silence the compiler, no per-call scratch state kept on the instance. |
| `ValueObjects` | give related data a type — an immutable `record` built at the edge — instead of a dictionary read by fixed string keys, or values that always travel together. |

### Frontend

| Skill | What it teaches |
|---|---|
| `MirroredServerType` | a hand-written TS type that mirrors a backend Data class is a duplicated contract — mark the Data class `#[TypeScript]`, generate the type, and import the generated one. |
| `VueComponents` | extract a component when template markup REPEATS, or when an element reaches DEEP into nested data — pass it the mid-object as a prop. |
| `VueControlFlow` | dispatch on a value with `<SwitchCase :value>` (a slot per case), never a `v-if`/`v-else-if` chain re-testing the same subject. |

### Python

| Skill | What it teaches |
|---|---|
| `Absence` | decide absence where the value is born — raise, return an empty collection, or a Null Object — instead of an `X \| None` every caller re-checks; never `or ""` a required value. |
| `BehaviourPerMethod` | a parameter that picks WHICH behaviour runs means two functions share one name — split them and let the call say which it wants, instead of passing a bare `True`. |
| `ClassLayout` | state at the top — constants, class attributes and fields above `__init__`, methods after. |
| `DependencyDirection` | a declared layer may only import the layers it declared it may use — down the stack, never back up, never sideways, and never in a cycle. |
| `Documentation` | concise, present-tense docstrings; rare comments; never narrate the past. |
| `Duplication` | a function body written twice becomes one shared function, parameterised by what differs. |
| `Enums` | a closed set of values is an `Enum` or `StrEnum` carrying the per-case knowledge as methods, not string constants compared at every call site. |
| `Exceptions` | raise named exceptions built by a classmethod factory, never swallow a failure, and keep the cause with `raise … from`. |
| `FixAtTheSource` | trace a value, an effect or a piece of state to where it starts, and fix it there. |
| `Flow` | check preconditions at the top and leave (`return`/`raise`/`continue`), keep the body flat, no `else` after an exit, dispatch instead of an `elif` ladder over one subject. |
| `MethodMood` | commands are imperatives (`hide()`), state predicates are questions (`is_hidden()`). |
| `PassTheObject` | demand the resolved object you need, not an id plus its container — the caller resolves once and passes the object (and owns the not-found failure). |
| `RepeatedCallHelper` | a keyword call, a guard or a type check written the same way at 2+ sites belongs as one named method on the type it is about. |
| `RoleVocabulary` | a keyed store / membership set / first-match dispatcher: name it `*Registry`/`*Set`/`*Resolver` and honour the contract — a registry `get` raises on a miss. |
| `TellDontAsk` | behaviour belongs with its data: move a loop over one object's collection onto that object, and replace an `isinstance` ladder with a method each type answers. |
| `Templates` | a multi-line string is a triple-quoted f-string that SHOWS its output, never a list of line fragments joined. |
| `TypeHonesty` | a type must not lie: don't fake optionality with `\| None` a value never is, or keep per-call scratch state on `self`. |
| `ValueObjects` | give related data a type — a frozen dataclass — instead of a dict with string keys passed around, or values that always travel together. |

### TypeScript

| Skill | What it teaches |
|---|---|
| `Absence` | model absence honestly — one spelling for missing, no `??` that invents a value, no `?.` on something always set. |
| `Duplication` | a function body written twice becomes one shared function or composable, parameterised by what differs. |


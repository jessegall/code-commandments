# routine

<!-- Wraps the profile's own `routine.md` when it is repeated at a stop. Holes: {profile}
     {routine}. Delete this file to stop repeating the routine. -->

The `{profile}` routine, every time you come to a stop:

{routine}

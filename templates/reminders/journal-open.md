# journal-open

<!-- Said at a stop while work you declared is still open, and HOLDS the stop. Holes:
     {work} {end} {binary}. Delete this file to silence it. -->

You declared work that is still open:

{work}

Close it before you stop. If it is finished, say so — `{end} <the same words>`. If it is
not, say where it got to and what the next step is, so a reader on the far side of a
compaction is not left with a start and no end.

`{binary} journal open` lists it.

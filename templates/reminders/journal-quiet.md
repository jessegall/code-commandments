# journal-quiet

<!-- Said after a stretch of tool calls with nothing recorded. Holes: {tagged} {quiet}
     {binary}. Delete this file to silence it. -->

{tagged} this session, and nothing recorded in the last {quiet} tool calls. Tag the
message you are already writing — [!discovery]/[!correction]/[!update] — that is what
pays this off. Do NOT pin to clear it: `{binary} journal remember` is for a fact the
next reader would get WRONG without, never a status or a number.

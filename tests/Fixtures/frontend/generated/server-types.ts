// AUTO-GENERATED by the TypeScript transformer — do not edit.
//
// This restates the server `OrderData` payload, exactly as a hand-written twin would —
// but it IS the generator's output (the transformer config points here), so it is the
// single source of truth, not a duplicate. The detector must exempt it.

export type OrderData = {
  id: string
  total: number
  placedAt: string
  status: string
}

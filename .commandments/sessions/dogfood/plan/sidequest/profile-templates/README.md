# profile-templates

Jesse: `orchestrate profile templates` offers default templates to start a document from, so a blank file is not the only way in. The scaffold asks the question; a template shows an answer somebody found worth keeping

# let an orchestrator session reference extra skills

Jesse: 'we could allow for customization ofc, adding skill references or something — but thats later.' Deliberately out of scope for the first bash build. The shape already permits it: a session's skills are files in its own folder, so a skills.conf naming extra references is additive. Do not let the isolation rule (SPEC.md 1a) harden into something that forbids it — 1a is about what is INHERITED by default, not about what a session may opt into.

- queued 2026-08-30 16:30

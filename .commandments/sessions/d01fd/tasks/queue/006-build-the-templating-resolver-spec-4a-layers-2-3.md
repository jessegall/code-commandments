# build the templating resolver (SPEC 4a layers 2-3)

Named-not-built by the bash builder, correctly: %/%(name)/names.conf/namespacing/computed keys/[ns.*] arrived as six successive design messages mid-build and is a subsystem on the scale of the hook or journal, not an addition to one. Layer 1 (print, do not state) IS built — help, journal menu and status all loop live files. Do this as its own dispatch, with SPEC 4a as the whole brief.

- queued 2026-08-30 16:59

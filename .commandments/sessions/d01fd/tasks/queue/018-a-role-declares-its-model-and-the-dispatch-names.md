# a role declares its model and the dispatch names it

SPEC 8 in the orchestrator repo. Deliberately NOT sent to the running builder — it is mid-Phase E and this is a new feature, not a correction. Queued so the design is settled before it is built, per the working model: research first, then tell the agent.

- queued 2026-08-30 19:29

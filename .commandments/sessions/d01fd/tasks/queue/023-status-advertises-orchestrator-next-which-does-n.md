# status advertises orchestrator next, which does not exist

Found by Jesse asking how to start the orchestrator. A fresh session's status says 'orchestrator next says what the walk expects of you' — and next is unbuilt (SPEC 8a), so it falls through to the help screen. The FIRST thing a new session says names a command that is not there. Either build next or stop advertising it; a tool that points at its own missing command teaches the reader to distrust its output.

- queued 2026-08-30 20:40

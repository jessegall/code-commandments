{
    "triggers": [
        "in my Python code order.status is a string and I check == 'paid' or == 'refunded' in several modules, is that ok",
        "should I use module-level constants PENDING = 'pending' or something else for statuses",
        "I compare shipment.kind against \"box\", \"pallet\" and \"crate\" in three different Python modules, should that be something else"
    ],
    "not": [
        "should create_shipment take street, city and postcode separately or an object"
    ]
}

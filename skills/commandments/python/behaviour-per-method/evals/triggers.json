{
    "triggers": [
        "should I add a `compact: bool = False` parameter to my Python `render(order)` function to switch between two layouts?",
        "my function's whole body is `if dry_run: ... else: ...` — is that fine or should it be split?",
        "I made `fields(kind: str | None = None)` so calling it with no kind returns all of them — good idea?"
    ],
    "not": [
        "how do I sort a list of dicts by a key in Python"
    ]
}
